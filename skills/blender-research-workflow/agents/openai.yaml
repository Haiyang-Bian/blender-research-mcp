interface:
  display_name: "Blender Research Workflow"
  short_description: "Launch, manage, diagnose, and preview Blender projects"
  default_prompt: "Use $blender-research-workflow to follow my Blender application and project intent, then diagnose or preview bounded LookDev changes as requested."
dependencies:
  tools:
    - type: "mcp"
      value: "blender_research"
      description: "Local semantic Blender Research MCP server"
policy:
  allow_implicit_invocation: true
