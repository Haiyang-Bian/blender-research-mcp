interface:
  display_name: "Blender Research Workflow"
  short_description: "Spatially diagnose and safely preview Blender scenes"
  default_prompt: "Use $blender-research-workflow to capture, spatially diagnose, and safely preview this Blender scene."
dependencies:
  tools:
    - type: "mcp"
      value: "blender_research"
      description: "Local semantic Blender Research MCP server"
policy:
  allow_implicit_invocation: true
