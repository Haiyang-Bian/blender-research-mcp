interface:
  display_name: "Blender Research Workflow"
  short_description: "Manage, inspect, model, and review Blender projects"
  default_prompt: "Use $blender-research-workflow to follow my Blender application and project intent, then inspect, author, or review bounded object, Mesh, Modifier, and material changes as requested."
dependencies:
  tools:
    - type: "mcp"
      value: "blender_research"
      description: "Local semantic Blender Research MCP server"
policy:
  allow_implicit_invocation: true
